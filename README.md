# Capy Ventas

Sistema de punto de venta (POS) para gestión de inventario, ventas y créditos.

---

## Cambios realizados mediante IA

| Modelo | Fecha y Hora | Cambio realizado |
|--------|-------------|-----------------|
| GEMINI ANTIGRAVITY | 2026-03-12 23:26 | Fix responsivo en la leyenda de colores del inventario (`Views/App/POS/Inventory/inventory.php` L41-L66): se eliminó `fs-6` de los badges, se añadió `flex-wrap`, el separador `vr` se oculta en móvil con `d-none d-sm-inline`, y los umbrales de stock/vencimiento se muestran en línea separada en móvil usando `w-100 d-sm-none` con fuente reducida a 0.78rem. |
| GEMINI ANTIGRAVITY | 2026-03-12 23:27 | Fix adicional: se añadió `overflow-x: auto` y `flex-wrap: wrap` al contenedor `div.row` de la leyenda como capa de seguridad para pantallas muy pequeñas donde el contenido aún pudiera desbordarse. |
| GEMINI ANTIGRAVITY | 2026-03-12 23:31 | Fix definitivo en leyenda de colores de inventario (`Views/App/POS/Inventory/inventory.php` L41-L66): se reemplazaron los `<span>` principales de cada badge por `<div>` (los `span` son inline por naturaleza y no respetan correctamente `d-flex`/`flex-wrap` en todos los navegadores). Se eliminó el sistema doble de spans desktop/mobile redundantes, simplificando a uno solo con `flex-wrap`. Se removieron inline styles usando la clase Bootstrap `small`. |
| GEMINI ANTIGRAVITY | 2026-03-17 21:44 | Creación de la vista `Views/qr.php` para el generador de códigos QR gratis. UI responsiva usando `qrcode.js` con opciones de color y descarga. Se adaptó el controlador `Controllers/Home.php` método `qr()` para incluir metas SEO. |
| GEMINI ANTIGRAVITY | 2026-03-17 21:50 | Se agregó la nueva sección de "Herramientas gratuitas" en el landing page (`Views/home.php`) enlazando al Generador de QR, y se actualizó el menú superior de ambas vistas (`home.php` y `qr.php`). |
| GEMINI ANTIGRAVITY | 2026-03-17 21:53 | Rediseño completo de `Views/qr.php`: Se añadieron pestañas (Tabs) para generar códigos QR avanzados (URL, Texto, WhatsApp, WiFi, vCard, Correo). Se mejoró el script JS para la correcta codificación de formatos y opciones de tamaño/color. Se fijaron los errores responsivos del menú móvil replicando la estructura del `home.php`. |
| GEMINI ANTIGRAVITY | 2026-03-17 21:57 | Reparación de los quiebres responsivos (CSS Media Queries) en `Views/qr.php`: Se restauró el comportamiento del Header envuelto (`flex-wrap`); se corrigió el overflow de las pestañas (`flex-shrink: 0`); y se añadió un media query para móviles pequeños (`max-width: 520px`) para las grillas de características y tamaños de fuente. |
| GEMINI ANTIGRAVITY | 2026-03-17 22:02 | Fix definitivo del overflow horizontal en móviles (`Views/qr.php`): Se añadió `min-width: 0` a los grid items de `.qr-hero` solucionando un bug nativo de CSS Grid que heredaba el ancho infinito `min-content` desde la botonera (Scroll de Tabs), lo que impedía que todo el bloque `.qr-form` se ajustara a la pantalla del celular. |
| GEMINI ANTIGRAVITY | 2026-03-19 22:10 | Fix al bug de encogimiento visual de pantalla al abrir modales: en `functions_inventory.js` se añadió `setTimeout` para limpiar `document.body.style.paddingRight` y dar tiempo a que termine la animación de cierre de SweetAlert antes de abrir el Modal de Bootstrap; y en `main.js` se colocó `scrollbarPadding: false` a las llamadas globales de loading para no generar espacios fantasma. |
| GEMINI ANTIGRAVITY | 2026-03-21 16:53 | Migración de datos sensibles: se trasladaron todas las variables del sistema (rutas, nombre local, zona horaria, apis, correo, encriptación) desde `Config.php` al archivo `.env` para mayor seguridad, adaptando `Config.php` para consumir dichos valores a través de `$_ENV`. |
| GEMINI ANTIGRAVITY | 2026-03-22 10:56 | Optimización de código en la función `render` de las acciones del inventario (`functions_inventory.js` L311-342): se refactorizó la lógica condicional para construir dinámicamente el grupo de botones basándose en los permisos (`row.update` y `row.delete`), eliminando renderizados HTML repetitivos y múltiples `if`. |
| GEMINI ANTIGRAVITY | 2026-03-22 10:58 | Mejora visual del badge "Sin permisos" en el inventario (`functions_inventory.js` L315): se reemplazaron las clases Bootstrap 4 redundantes (`badge-secondary bg-secondary`) por utilidades modernas de Bootstrap 5 (`bg-light text-muted border`) para darle una apariencia más sutil de elemento deshabilitado. |
| GEMINI ANTIGRAVITY | 2026-03-22 11:04 | Integración de un Preloader Full HTML/CSS premium (`Views/Template/panelPos/head.php` L74): se diseñó un loader a pantalla completa con efecto "Glassmorphism" (backdrop-filter: blur), una animación CSS personalizada de anillo giratorio (`spinRing`), texto pulsante, y bloqueo temporal del scroll (`overflow: hidden`) durante la carga de la página. |
| GEMINI ANTIGRAVITY | 2026-03-22 11:06 | Rediseño del Preloader (`Views/Template/panelPos/head.php` L74): se reemplazó el diseño de anillos CSS por uno basado en componentes nativos de Bootstrap 5 (`spinner-grow`) e integrando el logotipo de la aplicación (`capylg.png`) con una sutil animación de rebote, logrando una carga más fluida y alineada con el branding e identidad visual (UI) de Capy Ventas. |
| GEMINI ANTIGRAVITY | 2026-03-22 11:08 | Diseño responsivo y circular para logo del loader (`Views/Template/panelPos/head.php` L78): se transformó el logo en un elemento circular usando clases Bootstrap (`rounded-circle`, `shadow-sm`, `bg-white`, `border-primary`, `p-3`) y `object-fit: contain` con unidades `vw` para integrarse de forma adaptativa y premium a múltiples resoluciones móviles y mantener la proporción perfecta independientemente de la resolución de la imagen. |
| GEMINI ANTIGRAVITY | 2026-03-22 11:10 | Corrección de conflicto UI entre Preloader y SweetAlert (`Views/Template/panelPos/head.php` L74): se eliminó la inyección de `overflow: hidden !important` sobre el `body` ya que inutilizaba el cálculo nativo de padding de SweetAlert rompiendo las vistas. Además, se incrementó el `z-index` a `9999999` y se forzó `100vw` y `100vh` para sobreponerse de forma absoluta por encima de cualquier otro modal o alerta de carga concurrente. |
| GEMINI ANTIGRAVITY | 2026-03-24 20:57 | Mejora visual del bloque del código de barras en el modal de registro de productos (`Views/App/POS/Inventory/inventory.php` L199-L213): se envolvió el `<svg id="barcode">` en un contenedor estilizado con borde redondeado, etiqueta contextual con icono `bi-upc-scan`, y un estado vacío visible cuando aún no hay código ingresado. El SVG inicia oculto (`d-none`) y se muestra solo cuando hay un barcode válido renderizado. |
| GEMINI ANTIGRAVITY | 2026-03-25 12:05 | Mejora visual del render de `bar_code` en la tabla de historial del producto (`functions_inventory.js` L1046-1060): se reemplazaron los dos `<span>` planos sin estilo por un bloque `flex-column` con badge primario para el código (ícono `bi-upc`) y badge secundario pequeño para el formato, consistente con el estilo de la tabla principal de inventario. Se agregó guard para mostrar `-` cuando no hay código. |
| GEMINI ANTIGRAVITY | 2026-03-25 14:51 | Diseño completo del contenido del modal `#modalGenerateAllBarcodes` (`Views/App/POS/Inventory/inventory.php` L857-1003): se añadió sección de filtros tipo btn-check con 5 opciones (Todos, Retail, Alfanumérico, Logística, Industrial) con íconos y badges descriptivos, y una vista previa estilo hoja PDF con cabecera, grilla de 12 etiquetas de código de barras de muestra (SVG + nombre + código numérico), pie de página, y footer del modal con botones de Imprimir y Descargar PDF. |


