<?php
/**
 * Archivo de cabecera del módulo Clust.
 *
 * Documentación de estilos cargados automáticamente:
 * - style_clust.css: contiene la maquetación documentada del sidebar, utilidades
 *   generales y el visor de archivos descritos en la vista principal del
 *   módulo.
 *
 * Los recursos se inyectan desde `Views/Template/panel/head.php` mediante las
 * variables `page_container` y `page_js_css`, aplicando un control de versión
 * automático para garantizar que los estilos actualizados se descarguen en el
 * navegador sin pasos adicionales.
 */
